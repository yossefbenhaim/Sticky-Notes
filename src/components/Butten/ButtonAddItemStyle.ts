import { makeStyles } from "@mui/styles";

const usuStyle = makeStyles({
  buttenStyly: {
    backgroundColor: "red",
    width: "6rem",
    height: "3rem",
    fontSize: 13,
    margin: "3%",
  },
  colors: {
    backgroundColor: "red",
  },
});

export default usuStyle;
